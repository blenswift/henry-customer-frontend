import { ExtrasShortPipe } from './extras-short.pipe';

describe('ExtrasShortPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtrasShortPipe();
    expect(pipe).toBeTruthy();
  });
});
