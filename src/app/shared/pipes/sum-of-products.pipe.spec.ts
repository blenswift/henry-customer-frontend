import { SumOfProductsPipe } from './sum-of-products.pipe';

describe('SumOfProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new SumOfProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
