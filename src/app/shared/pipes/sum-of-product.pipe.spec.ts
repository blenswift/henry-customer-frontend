import { SumOfProductPipe } from './sum-of-product.pipe';

describe('SumOfProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SumOfProductPipe();
    expect(pipe).toBeTruthy();
  });
});
