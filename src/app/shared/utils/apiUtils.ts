export function getApiUrl(): string {
  /*if (window && window.location && window.location.hostname.includes('ngrok-free') || window.location.hostname === 'localhost') {
    return 'http://localhost:8080/v1';
  }*/
    return 'https://demo-api.henryapp.de/v1';
}

